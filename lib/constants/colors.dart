import 'package:flutter/material.dart';

// Primary Color (60% of the design)
const Color primaryColor = Color(0xFF41295A);

// Secondary Color (30% of the design)
const Color secondaryColor = Color(0xFF2F0743);

// Accent Color (10% of the design)
const Color accentColor = Color(0xFFEAEAEA);
const Color logo = Color(0xFFA280E2);

const Gradient gradient = LinearGradient(
    colors: [primaryColor, secondaryColor],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight);
